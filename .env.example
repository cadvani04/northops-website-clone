# Calendly (public — inline embed URL, e.g. https://calendly.com/you/30min)
NEXT_PUBLIC_CALENDLY_URL=

# Meta Pixel browser snippet (public; defaults to repo Pixel ID if unset)
NEXT_PUBLIC_META_PIXEL_ID=1536713821309167

# Meta Conversions API (server-only — never prefix with NEXT_PUBLIC_)
META_ACCESS_TOKEN=
META_PIXEL_ID=1536713821309167

# Optional: Events Manager → Test events code for CAPI debugging
META_TEST_EVENT_CODE=
